{
    "title" : "{{ .Title }}",
    "date" : "{{ .Date }}",
    "tags" : "{{ .Params.tags }}",
    "html" : "{{ with .OutputFormats.Get "HTML" }}{{ .Permalink }}{{ end }}",
    "json" : "{{ with .OutputFormats.Get "json" }}{{ .Permalink }}{{ end }}" 
}